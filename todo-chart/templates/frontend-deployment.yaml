apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "todo-chart.fullname" . }}-frontend
spec:
  replicas: {{ .Values.replicaCount.frontend }}
  selector:
    matchLabels:
      app: todo-frontend
  template:
    metadata:
      labels:
        app: todo-frontend
    spec:
      initContainers:
      - name: wait-for-api
        image: busybox
        command: ['sh', '-c', 'until nc -z todo-api 8080; do echo waiting for api; sleep 2; done;']
      containers:
      - name: frontend
        image: {{ .Values.images.frontend }}
        envFrom:
        - configMapRef:
            name: {{ include "todo-chart.fullname" . }}-config
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 30
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 30
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
